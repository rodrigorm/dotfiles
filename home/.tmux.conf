source-file "~/.tmux/themes/tokyonight_night.tmux"

# Reduce escape time (default 500ms interferes with Alt key combinations)
set -sg escape-time 10

# Enable extended keys for modifier passthrough
set -s extended-keys on

# Allow applications to receive extended key sequences
set -g allow-passthrough on
