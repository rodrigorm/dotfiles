#!/bin/sh

git branch > /dev/null;
if [ "$?" -eq "0" ]; then
	git status | grep "nothing to commit" > /dev/null;
	if [ ! "$?" -eq "0" ]; then
		git branch 2>/dev/null | grep '*' | sed 's/\* //';
	fi;
fi


