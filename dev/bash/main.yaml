depends:
  - homebrew

actions:
  - action: command.run
    where: os.name == "linux"
    command: brew
    args:
      - "unlink"
      - "util-linux"

  - action: command.run
    command: brew
    args:
      - "install"
      - "bash"
      - "bash-completion"
