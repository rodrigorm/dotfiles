depends:
  - dev.aptitude
  - dev.bash

actions:
  - action: package.install
    list:
     - "curl"

  - action: command.run
    command: sh
    args:
      - "-c"
      - "curl -fsSL https://pyenv.run | bash"
